# Copy this file to inputs.yaml and fill in your values.
# inputs.yaml is gitignored — do not commit it.
# Secrets go in your project .env (also gitignored), not here.
#
paypal_environment: "sandbox"
