{"hurdles":"PayPal sandbox REST apps require clicking through a confusing dual-account flow (personal vs business). The agent needed clarification that the sandbox app lives under the developer dashboard, not the main account settings.","improvements":"Add a HITL step that validates the sandbox client ID format before preflight runs — saves one full re-run when credentials are pasted with trailing whitespace."}
